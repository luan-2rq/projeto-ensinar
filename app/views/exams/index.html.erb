<%= render 'layouts/footer' %>

<div id="exams">
  <% @exams.each do |exam| %>
    <%= render exam %>
    <p>
      <%= link_to "Show this exam", exam %>
    </p>
  <% end %>
</div>

<%= link_to "New exam", new_exam_path %>

<%= render 'layouts/navbar' %>