<%= form_with url: "/questions/open_ended", method: :post do |form| %>
  <% if question.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
        <% question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, "Descrição", style: "display: block" %>
    <%= form.text_area :description %>
  </div>

    <div>
        <%= form.label :possible_reply, "Resposta Correta", style: "display: block" %>
        <%= form.text_field :possible_reply %>
    </div>

    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button class="btn btn-primary" type="submit">Adicionar questão</button>
        <%= link_to 'Criar prova', '/exams/new', class: "btn btn-primary" %>
    </div>
<% end %>
